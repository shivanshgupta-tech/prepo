services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: career_coach
    ports:
      - "5433:5432"
    volumes:
      - career_coach_pg:/var/lib/postgresql/data

volumes:
  career_coach_pg:
